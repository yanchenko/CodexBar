/* Empty translation unit so SwiftPM treats CAgentBar as a buildable C target
 * whose public header (include/agentbar.h) is importable from Swift. The actual
 * symbols are provided by the linked Rust staticlib libab_core.a. */
