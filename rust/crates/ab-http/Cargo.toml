[package]
name = "ab-http"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "AgentBar blocking HTTPS client (rustls)."

[dependencies]
attohttpc = { workspace = true }
# HeaderName for dynamic request headers (attohttpc re-exports http types transitively;
# pin a direct dep so we can construct owned names).
http = "1"

[dev-dependencies]
httpmock = { workspace = true }

[lints]
workspace = true
